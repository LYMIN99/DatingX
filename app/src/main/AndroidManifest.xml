<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lymindev.datingx">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activities.signup.SignUpStepFiveLocationActivity"></activity>
        <activity android:name=".view.activities.signup.SignUpStepFourProfileActivity" />
        <activity android:name=".view.activities.signup.SignUpStepThreeDobActivity" />
        <activity android:name=".view.activities.signup.SignUpStepThowNameActivity" />
        <activity android:name=".view.activities.signup.SignUpStepOneGanderActivity" />
        <activity android:name=".view.activities.login.PhoneLoginActivity" />
        <activity android:name=".view.activities.user.EditInfoActivity" />
        <activity android:name=".view.activities.me.MyProfileActivity" />
        <activity android:name=".view.activities.user.UserProfileActivity" />
        <activity android:name=".view.activities.login.LoginActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".view.activities.StepperWizardColor" />
    </application>

</manifest>